branches:
  only:
    - travis
language: cpp
compiler:
  - gcc
before_script:
  - rake build:all --trace
  - ./autogen.sh && ./configure --enable-unittests CFLAGS="-Ibuild/include" LD_FLAGS="-Lbuild/lib/"
script: make
